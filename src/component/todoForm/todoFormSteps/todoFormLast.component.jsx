import React from "react";
import { connect } from "react-redux";

const todoFormLast = () => {
  return <div></div>;
};

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(todoFormLast);
