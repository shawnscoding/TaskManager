import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  buttonContainer: {
    marginTop: 20
  },
  paper: {
    padding: "20px",
    marginTop: "5px",
    height: "53vh",
    overflowY: "auto"
  }
});
